import org.scalatest.FunSuite

import gov.ornl.stucco.rt.bolt.ConceptBolt

import backtype.storm.tuple.Values

class ConceptBoltSuite extends FunSuite {

  val bolt = new ConceptBolt

  test("concept extraction works correctly") {
    (pending)
  }
}
