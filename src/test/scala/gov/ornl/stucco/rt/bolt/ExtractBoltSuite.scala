import org.scalatest.FunSuite

import gov.ornl.stucco.rt.bolt.ExtractBolt

import backtype.storm.tuple.Values

class ExtractBoltSuite extends FunSuite {

  val bolt = new ExtractBolt

  test("unstructured text processed correctly") {
    (pending)
  }
}
