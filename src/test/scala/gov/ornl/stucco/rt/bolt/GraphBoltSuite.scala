import org.scalatest.FunSuite

import gov.ornl.stucco.rt.bolt.GraphBolt

import backtype.storm.tuple.Values

class GraphBoltSuite extends FunSuite {

  val bolt = new GraphBolt

  test("graph merging works correctly") {
    (pending)
  }

  test("graph insertion works correctly") {
    (pending)
  }
}
