import org.scalatest.FunSuite

import gov.ornl.stucco.rt.bolt.ParseBolt

import backtype.storm.tuple.Values

class ParseBoltSuite extends FunSuite {

  val bolt = new ParseBolt

  test("parsing works correctly") {
    (pending)
  }
}
