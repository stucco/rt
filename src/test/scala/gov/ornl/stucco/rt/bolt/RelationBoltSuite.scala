import org.scalatest.FunSuite

import gov.ornl.stucco.rt.bolt.RelationBolt

import backtype.storm.tuple.Values

class RelationBoltSuite extends FunSuite {

  val bolt = new RelationBolt

  test("relation extraction works correctly") {
    (pending)
  }
}
