import org.scalatest.FunSuite

import gov.ornl.stucco.rt.bolt.SplitBolt

import backtype.storm.tuple.Values

class SplitBoltSuite extends FunSuite {

  val bolt = new SplitBolt

  test("splitting works correctly") {
    (pending)
  }
}
