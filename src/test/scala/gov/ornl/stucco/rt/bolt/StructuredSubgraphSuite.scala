import org.scalatest.FunSuite

import gov.ornl.stucco.rt.bolt.StructuredGraphBolt

import backtype.storm.tuple.Values

class StructuredGraphBoltSuite extends FunSuite {

  val bolt = new StructuredGraphBolt

  test("subgraph building for structured data works correctly") {
    (pending)
  }
}
