import org.scalatest.FunSuite

import gov.ornl.stucco.rt.bolt.UnstructuredGraphBolt

import backtype.storm.tuple.Values

class UnstructuredGraphBoltSuite extends FunSuite {

  val bolt = new UnstructuredGraphBolt

  test("subgraph building for unstructured data works correctly") {
    (pending)
  }
}
